# WBF-24 框与类别数量不一致异常

状态：已复现。实际为SystemExit(None)，错误信息打印到stdout；直接运行的进程可能以成功状态退出。与23、30按exit调用同类根因归并，接口契约待评审。

预期依据：建议接口契约：无效输入应抛出可由调用方处理的ValueError，不能直接终止宿主进程。此契约尚待评审，实际退出行为单独记录。

输入、实际输出、复现命令、环境与日志见[测试记录](../../manual_tests/WBF-24.md)。

修复状态：未修复；修复提交和修改版回归待后续补充。保持原版失败断言，不使用xfail。
