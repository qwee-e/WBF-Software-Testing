# WBF-28 全零模型权重异常

状态：已复现。实际返回NaN坐标和NaN分数，并有除零相关警告。坐标除以贡献和0，分数又除以权重和0，缺少无效权重保护。与26存在零贡献除零的共同路径。

预期依据：权重和为0，归一化无定义；本测试采用明确抛出ValueError的建议契约，待评审。不得静默返回NaN或Inf。

输入、实际输出、复现命令、环境与日志见[测试记录](../../manual_tests/WBF-28.md)。

修复状态：未修复；修复提交和修改版回归待后续补充。保持原版失败断言，不使用xfail。
