# weighted_boxes_fusion 人工源码分析

分析对象：`ensemble_boxes/ensemble_boxes_wbf.py`，以 `reproduction-success` 基线为对照。
本阶段只阅读源码、推导接口行为，不修改算法、不新增或执行测试。本记录不是测试执行结果；下文的“建议约定”也不是原项目已经提供的保证。

## 1. 输入格式与合法范围

设模型数为 M，第 t 个模型给出 N_t 个框。不同模型的框数可以不同，不要求把输入组成规则的三维 NumPy 数组。

| 参数 | 格式与建议合法范围 | 默认值与源码行为 |
| --- | --- | --- |
| `boxes_list` | 按模型分组的列表；第 t 组有 N_t 个框，每框恰为 `[x1,y1,x2,y2]`。坐标为有限数值，归一化到 `[0,1]`，有效框满足 `x1<x2`、`y1<y2` | 必填；支持空列表和空模型。原代码会修正反向、越界坐标，并丢弃修正后零面积框 |
| `scores_list` | 按模型分组，与框逐项对应；置信度建议为有限实数，范围 `[0,1]` | 必填；源码没有检查分数范围或有限性。零分框可以进入算法，但零加权分数的融合簇可能除零 |
| `labels_list` | 按模型分组，与框逐项对应；建议使用整数类别编号，各模型必须采用同一类别映射 | 必填；源码使用 `int()` 转换，没有规定类别上限，也没有禁止负整数。非整数小数会被截断，不应把这种转换视为规范输入 |
| `weights` | 一维模型权重列表，长度为 M；为避免除零及负权重，建议每项为有限正数。无需和为 1 | `None`：全 1；长度错误：打印警告后整体回退为全 1。源码没有约束权重符号、有限性或数组维数 |
| `iou_thr` | 建议为 `[0,1]` 内的有限实数 | `0.55`；源码不校验范围。匹配要求 `IoU > iou_thr` |
| `skip_box_thr` | 建议为 `[0,1]` 内的有限实数 | `0.0`；源码不校验范围。仅丢弃原始分数严格小于此值的框 |
| `conf_type` | 必须是 `'avg'`、`'max'`、`'box_and_model_avg'`、`'absent_model_aware_avg'` 之一 | `'avg'`；非法值打印错误后调用 `exit()` |
| `allows_overflow` | 建议严格使用布尔值 | `False`；只影响 `avg` 的最终分数缩放，其他三种模式不使用它。源码按真假值判断，没有类型检查 |

必须满足的对应关系：

```text
len(boxes_list) == len(scores_list) == len(labels_list) == M
对每个 t：len(boxes_list[t]) == len(scores_list[t]) == len(labels_list[t])
weights 为 None，或 len(weights) == M
```

最小的双模型输入示意：

```python
boxes_list = [[[0.1, 0.1, 0.5, 0.5]], [[0.1, 0.1, 0.5, 0.5]]]
scores_list = [[0.9], [0.8]]
labels_list = [[1], [1]]
weights = [1, 1]
```

按默认参数手工推导：两框完全重合且类别相同，融合坐标仍为 `[0.1,0.1,0.5,0.5]`，分数约为 `0.85`，类别为 `1`。这是源码推导示例，不是本阶段执行的测试结果。

## 2. 输出格式

返回顺序固定为 `(boxes, scores, labels)`，均为 NumPy 数组。设最终保留 K 个融合簇：

- `boxes` 形状为 `(K,4)`，仍按 `[x1,y1,x2,y2]` 排列。
- `scores` 形状为 `(K,)`，是按 `conf_type` 计算并缩放后的置信度。
- `labels` 形状为 `(K,)`，与框和分数逐项对应。由于内部共用浮点数组，返回类别通常是浮点表示，例如 `1.0`，而非整数数组。
- 输出按最终置信度降序排列；同分时源码未约定稳定的先后次序。
- 没有有效框时返回形状为 `(0,4)`、`(0,)`、`(0,)` 的三个空数组。
- 单个框也形成一个簇：坐标保持预处理后的值，但分数仍会缩放，不一定等于原始分数。

在有限正权重、有效坐标及正的簇加权分数总和条件下，融合坐标是输入坐标的加权平均，仍在 `[0,1]` 内。源码不保证任意异常输入都得到有限、有效输出。

## 3. 哪些框应该融合

源码的处理顺序如下：

1. 检查每个模型内部的框、分数和类别数量。
2. 先按原始分数过滤，再转换类别和坐标、修正坐标、去掉零面积框。
3. 按类别分组，每组按 `原始分数 × 模型权重` 降序处理。
4. 对每个候选框，计算它与已有各簇“当前融合框”的 IoU。
5. 选择 IoU 最大的簇；只有最大 IoU 严格大于阈值时才加入，否则新建簇。
6. 每加入一个框，就用该簇全部原始成员重新计算加权融合框。
7. 聚类结束后缩放置信度，再把所有类别的结果合并并按最终分数降序返回。

IoU 定义为 `交集面积 / 并集面积`。框的坐标连续计算，不使用像素框公式中的 `+1`。

| 情况 | 按当前源码应有的行为 |
| --- | --- |
| 同类别，和最佳当前融合框的 IoU 大于阈值 | 加入该簇 |
| 同类别，最大 IoU 恰等于阈值或小于阈值 | 不融合，新建簇 |
| 不同类别，即使坐标完全相同 | 不融合 |
| 同一模型给出的同类别重叠框 | 可以融合；没有“每模型每簇最多一个框”的限制 |
| 不同模型给出的框 | 仍须满足同类别和 IoU 条件，不能仅因来自不同模型就融合 |
| 分数小于 `skip_box_thr` | 预先丢弃，不参与聚类，也不作为独立输出 |
| 分数等于 `skip_box_thr` | 保留并继续后续检查 |
| 零面积框，包括裁剪后变为零面积的框 | 发出警告并丢弃 |
| 同类完全重合框，IoU=1 | 阈值小于 1 时可融合；阈值等于 1 时不融合 |
| 没有交集或仅边界接触的框，IoU=0 | 在合法的非负阈值下不融合，包括阈值为 0 的情况 |
| 同时与多个簇达到匹配条件 | 只加入 IoU 最大的一个簇，不合并多个已有簇 |

关键理解：这是按分数排序、逐步更新的贪心聚类，不是对所有原始框进行固定的两两分组。A 与 B 重叠、B 与 C 重叠，不足以断定三者一定融合；C 要与当时的融合框比较。也不能要求同簇全部原始成员两两 IoU 都超过阈值。

模型权重会影响处理次序和融合框坐标，从而可能间接影响后续分组。相同加权分数下的次序没有稳定性保证。

## 4. 融合坐标与分数如何计算

对于一个簇，设框数为 k、框 j 的原始分数为 s_j、所属模型权重为 w_j，定义：

```text
S = Σ(s_j × w_j)                        # 簇内加权分数之和
W_box = Σw_j                           # 按框计权重，同一模型可重复
W_all = 所有输入模型的权重之和
W_present = 此簇出现过的不同模型的权重之和
W_absent = 此簇未出现的模型的权重之和
M = 输入模型总数
```

融合坐标的四个分量分别计算：`Σ(s_j × w_j × 坐标_j) / S`。因此 S 必须大于 0 才能正常求这个加权平均。`conf_type='max'` 也使用此坐标公式，并非直接取最高分框坐标。

最终分数按代码代数整理如下（忽略浮点舍入）：

| 配置 | 最终分数 |
| --- | --- |
| `avg` 且 `allows_overflow=False` | `(S/k) × min(M,k) / W_all` |
| `avg` 且 `allows_overflow=True` | `S / W_all` |
| `max` | `max(s_j × w_j) / max(所有模型权重)` |
| `box_and_model_avg` | `(S / W_box) × (W_present / W_all)` |
| `absent_model_aware_avg` | `S / (W_box + W_absent)` |

空模型仍计入输入模型总数和全体模型权重，可能影响最终分数。`skip_box_thr` 在缩放前过滤，源码没有再用这个阈值过滤输出分数。

**`allows_overflow=False` 并不是最终分数的硬截断。** 源码没有 `clip(score,0,1)`。例如两个模型权重为 `[2,1]`，第一模型有两个同类别完全重合、分数均为 0.9 的框，第二模型为空；默认 `avg` 下 k=2、M=2、S=3.6、W_all=3，输出分数按公式为 `1.2`。这是静态推导出的待验证风险，不应因参数名就断言分数一定不超过 1。

## 5. 异常输入：实际行为与建议约定

| 异常情况 | 当前源码实际行为或可推导风险 | 建议后续明确的接口约定 |
| --- | --- | --- |
| `weights` 长度错误 | 打印警告，改用全 1，继续运行 | 基线测试应记录这一回退行为；若改为严格拒绝，需明确为接口行为变更 |
| 某模型内框与分数/类别数量不一致 | 打印错误并 `exit()`；通常表现为 `SystemExit(None)`，不是 `ValueError` | 库函数应抛出明确的 `ValueError`，不要退出调用者进程 |
| 三个列表的模型数不一致 | 没有统一外层校验；分数/类别少了可能 `IndexError`，多余部分可能被忽略 | 在入口检查外层长度，抛出 `ValueError` |
| 不支持的 `conf_type` | 打印错误并 `exit()` | 抛出 `ValueError`，列出支持值 |
| x/y 端点顺序相反 | `warnings.warn`，交换端点 | 可保留现有修正行为并要求发出警告 |
| 坐标小于 0 或大于 1 | `warnings.warn`，逐坐标裁剪到 `[0,1]` | 可保留现有裁剪行为；它不是把像素坐标按图像宽高归一化 |
| 修正后零面积 | 警告并丢弃 | 保留该行为 |
| 单框不足 4 个坐标或多于 4 个坐标 | 被处理时少于 4 项可能 `IndexError`；多于 4 项只取前四项 | 校验每框恰好 4 个坐标，错误时 `ValueError` |
| 类别为小数、字符串或不可转换对象 | 调用 `int()`，可能静默截断、接受数字字符串或产生转换异常 | 明确整数类别契约，拒绝非整数类别，避免误归类 |
| 负分数、分数超过 1、非数值分数 | 没有范围校验；负分可能先被过滤，超过 1 的分数可能进入计算；非数值可能发生比较或转换错误 | 分数应为 `[0,1]` 内的有限数值；类型错误用 `TypeError`，范围错误用 `ValueError` |
| 非正权重、权重和为 0 | 未校验，可能导致负分、分母为零或无效坐标 | 本阶段建议安全输入域为正权重；若要支持 0 权重屏蔽模型，必须另行明确忽略规则 |
| 分数或权重含 NaN/Inf | 没有统一有限性检查，可能污染排序、坐标和输出分数 | 显式检查有限性并拒绝 |
| 坐标含 NaN/Inf | NaN 不会被普通大小比较可靠排除；正负 Inf 会触发越界裁剪，可能随后丢弃零面积框 | 明确拒绝非有限坐标，避免不一致的隐式处理 |
| 阈值越界或 NaN | 没有范围校验；例如负 IoU 阈值可使 IoU=0 的同类框融合，IoU 阈值大于 1 则无法匹配 | 两阈值应为 `[0,1]` 内的有限实数 |
| `allows_overflow` 不是布尔值 | 按 Python 真假值分支，多元素数组等还可能报错 | 明确要求布尔类型 |
| 零分数框、零加权分数簇 | 默认过滤阈值为 0，零分框可保留；多个这样的框融合时 S=0，坐标计算可能出现 NaN | 需在设计测试前约定丢弃零贡献框，或给零和簇定义明确行为，不能默认已有保护 |
| 空输入或所有框被过滤 | 返回三个空数组 | 属于正常边界，应正常返回 |

异常发生还与处理顺序有关：低分框在读取类别、坐标之前就被跳过，因此低分框中的坏坐标未必触发报错。但每个模型内部的数量检查发生在逐框分数过滤之前。

## 6. 后续测试设计应依据什么

已有明确行为可直接作为预期：输入输出形状、空结果、类别隔离、IoU 严格大于阈值、分数过滤的严格小于关系、坐标修正、零面积丢弃、同模型可融合以及上述分数公式。

对于缺少统一约定的输入，应将“记录基线现状”和“验证改进后的接口契约”分开。`SystemExit`、非有限值、零权重/零分数、非整数类别和 `allows_overflow=False` 仍可能超 1 等问题，需要后续用独立测试验证，不能在本阶段宣称已经复现 Bug 或已经修复。
