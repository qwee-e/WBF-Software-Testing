# 模块二执行记录

本轮范围为 AI-WBF-001、002、004、005、006、008、014、016。逐条实现、运行、保存证据后分别提交并推送。
不修改被测源码，不把采样通过解释为算法正确性的证明，不把已知缺陷算作新发现。

|用例|实际执行|结果|结论与证据|
|---|---|---|---|
|001 模型排列|200 组生成输入 + 8 个组合锚点；1,756 次非恒等排列比较；1,964 次 WBF 调用|PASS|强分离且加权分数无并列时，模型与权重同步重排不改变输出。[证据](results/AI-WBF-001/summary.md)|
|002 框排列|208 组输入；1,863 次非恒等排列比较；2,071 次调用|PASS|模型内部框/分数/标签同步排列，输出元组集合一致；并列分数桥接情形不混入严格断言。[证据](results/AI-WBF-002/summary.md)|
|004 整体平移|308 组输入；924 次平移比较；1,232 次调用|PASS|所有变换保持在归一化范围内，输出坐标同步平移，分数及标签不变。[证据](results/AI-WBF-004/summary.md)|
|005 整体缩放|308 组输入；924 次比例变换比较；1,232 次调用|PASS|每组包含放大及缩小，不裁剪坐标；输出几何同比变化，分数及类别不变。[证据](results/AI-WBF-005/summary.md)|
|006 水平镜像|308 组输入；616 次镜像/复原比较；924 次调用|PASS|镜像输出与先融合再镜像一致；两次镜像恢复原输出。[证据](results/AI-WBF-006/summary.md)|
|008 低分框注入|308 组输入；累计注入 46,548 个框；924 次比较；1,232 次调用|PASS|不增加模型和权重槽位，所有新增框严格低于过滤阈值，原输出不变。[证据](results/AI-WBF-008/summary.md)|
|014 桥接簇搜索|150 个场景；3,000 次排列；3,150 次确定性检查；3,000 次强分离对照；12,642 次总调用|硬断言 PASS；观察到敏感性|1,476 次同分排列改变输出，150 个场景均出现；去并列的 3,000 次比较无差异。缩减为 3 框见证，不直接认定新 Bug。[证据](results/AI-WBF-014/summary.md)|

001 初轮生成未命中一个模式/开关组合，因此在公共框架加入 8 个显式锚点后重新执行；表中为最终实测结果。
公共框架包括同步输入变换、强分离样本生成、元组一对一比较、环境记录及日志保存。
详细输入和输出保留在对应 `evidence.json`；固定 Hypothesis 版本与种子用于完整重放。

## 014 的观察如何解释

三个同类等分框呈链式重叠。改变到达次序后，贪心算法可能先融合左侧两个，也可能先融合右侧两个。
置信度未变，但最终框位置不同；同一精确输入始终得到相同结果。因此它反驳的是“所有合法输入都无条件排列不变”这一测试假设。
严格测试仍要求无并列、强分离条件下排列不变，本轮全部满足。
缩减器用 19 次候选删除、38 次 WBF 调用，保留 3 个框；继续删除其中任一个就无法保留输出差异。
这是对当前删除操作的 1-minimal，不代表对数值和所有模型结构全局最小。

快速重放已保存的最小见证：

```powershell
.\.venv-ai\Scripts\python.exe replay_ai_evidence.py docs/module2/results/AI-WBF-014/evidence.json
```

输出 `expected_order_sensitivity_reproduced: true` 代表成功重现这条观察，退出码为 0；它不是新增缺陷的判定。
