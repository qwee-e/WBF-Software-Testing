# 测试数据

本目录存放人工测试使用的输入数据和预期结果数据。

- 数据文件应注明对应的测试用例编号和用途。
- WBF 输入应清楚区分 `boxes_list`、`scores_list`、`labels_list` 及可选参数。
- 正常输入、边界输入和异常输入应明确标注；异常数据是有意构造的测试输入。
- 预期结果应注明依据，例如人工计算或已确认的接口约定，不能直接将程序实际输出当作预期结果。

当前仅建立目录及用途说明，尚未添加测试数据。
